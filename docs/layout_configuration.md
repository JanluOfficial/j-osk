# Layout Configuration
Keyboard layouts are defined as JSON files

## Config Version ("cversion")
The config version is a number that indicates the version of the layout configuration format. It is used to ensure that layouts stay compatible in the event of changes to the format.

```json
{
  "cversion": 1,
  ...
}
```